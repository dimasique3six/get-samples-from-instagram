# 🎵 Instagram Samples to Telegram Bot

Автоматический трансфер музыкальных сэмплов (reels) из чата Instagram в канал Telegram.

---

## 📋 Оглавление

1. [Подготовка](#1-подготовка)
2. [Установка на сервер](#2-установка-на-сервер)
3. [Настройка бота](#3-настройка-бота)
4. [Запуск как службы](#4-запуск-как-службы)
5. [Управление ботом](#5-управление-ботом)
6. [Решение проблем](#6-решение-проблем)

---

## 1. Подготовка

### 1.1 Создание Telegram бота

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Введите имя бота (например: `Samples Bot`)
4. Введите username бота (например: `my_samples_bot`)
5. **Сохраните токен** (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 1.2 Настройка Telegram канала

1. Создайте канал в Telegram (или используйте существующий)
2. Зайдите в настройки канала → Администраторы
3. Добавьте вашего бота в качестве администратора
4. Дайте боту права на **публикацию сообщений**

### 1.3 Получение ID канала

**Способ 1 - через @userinfobot:**
1. Перешлите любое сообщение из канала боту [@userinfobot](https://t.me/userinfobot)
2. Скопируйте ID канала (формат: `-100XXXXXXXXXX`)

**Способ 2 - через @raw_data_bot:**
1. Перешлите сообщение из канала боту [@raw_data_bot](https://t.me/raw_data_bot)
2. В ответе найдите поле `"id"` в разделе `"forward_from_chat"`

**Способ 3 - через web.telegram.org:**
1. Откройте канал на [web.telegram.org](https://web.telegram.org)
2. ID канала будет в URL (например: `web.telegram.org/#/im?p=c1234567890`)
3. Добавьте `-100` перед числом: `-1001234567890`

### 1.4 Получение Instagram Thread ID

1. Откройте Instagram в браузере
2. Перейдите в Директ → откройте нужный чат с сэмплами
3. Скопируйте число из URL после `/t/`
   - Пример URL: `https://www.instagram.com/direct/t/340282366841710301281176539621804876514/`
   - Thread ID: `340282366841710301281176539621804876514`

---

## 2. Установка на сервер

### 2.1 Подключение к серверу

```bash
ssh username@your-server.com
```

### 2.2 Создание директории

```bash
mkdir -p ~/instagram-samples-bot
cd ~/instagram-samples-bot
```

### 2.3 Загрузка файлов

**Вариант А - Скачать файлы с локального компьютера:**

```bash
# На вашем компьютере (в папке с файлами):
scp instagram-samples-bot.tar.gz username@your-server.com:~/

# На сервере:
tar -xzf instagram-samples-bot.tar.gz
cd instagram-samples-bot
```

**Вариант Б - Создать файлы вручную:**

```bash
# На сервере создайте файлы через nano или vim
nano instagram_samples_bot.py
# Скопируйте содержимое файла

nano install.sh
# Скопируйте содержимое файла

nano instagram-samples.service
# Скопируйте содержимое файла
```

### 2.4 Установка зависимостей

```bash
# Сделайте скрипт установки исполняемым
chmod +x install.sh

# Запустите установку
./install.sh
```

**Или установите вручную:**

```bash
# Обновление системы
sudo apt update
sudo apt install -y python3 python3-pip python3-venv yt-dlp

# Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate

# Установка Python библиотек
pip install -r requirements.txt
```

---

## 3. Настройка бота

### 3.1 Редактирование конфигурации

Откройте файл для редактирования:

```bash
nano instagram_samples_bot.py
```

### 3.2 Заполните следующие параметры:

```python
# Instagram данные
INSTAGRAM_USERNAME = "ваш_логин"          # Ваш логин Instagram
INSTAGRAM_PASSWORD = "ваш_пароль"        # Ваш пароль Instagram
INSTAGRAM_THREAD_ID = "340282366..."     # ID чата с сэмплами

# Telegram данные
TELEGRAM_BOT_TOKEN = "1234567890:ABC..." # Токен бота от BotFather
TELEGRAM_CHANNEL_ID = "-100123456789"    # ID канала (начинается с -100)

# Настройки
CHECK_INTERVAL = 60  # Интервал проверки новых сэмплов (в секундах)
```

Сохраните файл: `Ctrl+X`, затем `Y`, затем `Enter`

### 3.3 Тестовый запуск

Проверьте работу бота перед настройкой службы:

```bash
# Активируйте виртуальное окружение
source venv/bin/activate

# Запустите бота
python3 instagram_samples_bot.py
```

**Что должно произойти:**
- ✅ Бот авторизуется в Instagram
- ✅ При первом запуске пометит существующие сообщения
- ✅ Начнёт мониторинг новых сэмплов
- ✅ При появлении сэмпла скачает и опубликует в канал

**Остановка:** `Ctrl+C`

---

## 4. Запуск как службы

### 4.1 Настройка systemd service

Отредактируйте файл службы:

```bash
nano instagram-samples.service
```

**Замените `YOUR_USERNAME` на ваше имя пользователя:**

```ini
User=YOUR_USERNAME
WorkingDirectory=/home/YOUR_USERNAME/instagram-samples-bot
ExecStart=/home/YOUR_USERNAME/instagram-samples-bot/venv/bin/python3 /home/YOUR_USERNAME/instagram-samples-bot/instagram_samples_bot.py
StandardOutput=append:/home/YOUR_USERNAME/instagram-samples-bot/service.log
StandardError=append:/home/YOUR_USERNAME/instagram-samples-bot/service.log
```

### 4.2 Установка службы

```bash
# Скопируйте файл службы в systemd
sudo cp instagram-samples.service /etc/systemd/system/

# Обновите systemd
sudo systemctl daemon-reload

# Включите автозапуск при старте системы
sudo systemctl enable instagram-samples

# Запустите службу
sudo systemctl start instagram-samples
```

### 4.3 Проверка статуса

```bash
# Проверка статуса службы
sudo systemctl status instagram-samples

# Должно быть:
# ● instagram-samples.service - Instagram Samples to Telegram Bot
#    Loaded: loaded (/etc/systemd/system/instagram-samples.service)
#    Active: active (running) since ...
```

---

## 5. Управление ботом

### 5.1 Основные команды

```bash
# Запуск службы
sudo systemctl start instagram-samples

# Остановка службы
sudo systemctl stop instagram-samples

# Перезапуск службы
sudo systemctl restart instagram-samples

# Статус службы
sudo systemctl status instagram-samples

# Отключение автозапуска
sudo systemctl disable instagram-samples

# Включение автозапуска
sudo systemctl enable instagram-samples
```

### 5.2 Просмотр логов

```bash
# Логи службы (последние 50 строк)
sudo journalctl -u instagram-samples -n 50

# Логи в реальном времени
sudo journalctl -u instagram-samples -f

# Логи приложения
tail -f ~/instagram-samples-bot/samples_bot.log

# Логи службы
tail -f ~/instagram-samples-bot/service.log
```

### 5.3 Обновление конфигурации

После изменения настроек в `instagram_samples_bot.py`:

```bash
# Перезапустите службу
sudo systemctl restart instagram-samples

# Проверьте статус
sudo systemctl status instagram-samples
```

---

## 6. Решение проблем

### 6.1 Бот не запускается

**Проверьте логи:**
```bash
sudo journalctl -u instagram-samples -n 100
```

**Частые причины:**
- ❌ Неверный путь в service файле → проверьте пути
- ❌ Нет прав на выполнение → `chmod +x instagram_samples_bot.py`
- ❌ Виртуальное окружение не активировано → проверьте путь к python в ExecStart

### 6.2 Ошибка авторизации Instagram

**Симптомы:**
```
LoginRequired: Требуется повторная авторизация
```

**Решение:**
```bash
# Удалите старую сессию
cd ~/instagram-samples-bot
rm instagram_session.json

# Перезапустите службу
sudo systemctl restart instagram-samples
```

**Instagram может запросить подтверждение через приложение или SMS**

### 6.3 Бот не находит сэмплы

**Проверьте:**
1. Thread ID правильный
2. В чате есть новые сэмплы (не обработанные ранее)
3. Логи показывают получение сообщений

```bash
tail -f samples_bot.log
```

### 6.4 Ошибка отправки в Telegram

**Проверьте:**
- ✅ Бот добавлен в канал как администратор
- ✅ У бота есть права на публикацию
- ✅ Channel ID начинается с `-100`
- ✅ Токен бота правильный

**Тест отправки:**
```python
# Быстрый тест через Python
python3 -c "
import telegram
import asyncio
bot = telegram.Bot(token='ВАШ_ТОКЕН')
asyncio.run(bot.send_message(chat_id='-100ВАШИХ_ID', text='🎵 Тест'))
"
```

### 6.5 Служба постоянно перезапускается

```bash
# Проверьте логи ошибок
sudo journalctl -u instagram-samples -n 200 | grep ERROR

# Проверьте системные логи
sudo tail -f /var/log/syslog
```

### 6.6 Высокое использование ресурсов

Отредактируйте `CHECK_INTERVAL`:

```python
CHECK_INTERVAL = 120  # Увеличить до 2 минут
```

---

## 📊 Структура файлов

```
~/instagram-samples-bot/
├── instagram_samples_bot.py   # Основной скрипт бота
├── instagram_session.json     # Сессия Instagram (создаётся автоматически)
├── state.json                 # Состояние обработанных сообщений
├── samples_bot.log            # Логи приложения
├── service.log                # Логи службы
├── downloads/                 # Временная папка для скачанных сэмплов
├── venv/                      # Виртуальное окружение Python
├── install.sh                 # Скрипт установки
└── instagram-samples.service  # Файл systemd службы
```

---

## ⚙️ Дополнительные настройки

### Изменение интервала проверки

Более частая проверка (каждые 30 секунд):
```python
CHECK_INTERVAL = 30
```

Более редкая проверка (каждые 5 минут):
```python
CHECK_INTERVAL = 300
```

### Изменение формата подписи

Отредактируйте функцию `process_message`:

```python
# Простая подпись
caption = f"@{sender_name}"

# Подробная подпись
caption = f"🎵 Сэмпл от @{sender_name}\n{caption_text[:500]}"

# Без указания автора
caption = caption_text[:300] if caption_text else "🎵"
```

---

## 🆘 Получение помощи

Если возникли проблемы:

1. **Проверьте логи:**
   ```bash
   sudo journalctl -u instagram-samples -n 200
   tail -f samples_bot.log
   ```

2. **Проверьте статус:**
   ```bash
   sudo systemctl status instagram-samples
   ```

3. **Ручной запуск для отладки:**
   ```bash
   cd ~/instagram-samples-bot
   source venv/bin/activate
   python3 instagram_samples_bot.py
   ```

---

## 📝 Примечания

**Назначение:** Этот бот предназначен для автоматической пересылки музыкальных сэмплов (видео с треками) из Instagram чата в Telegram канал.

**Важно:** Частое использование Instagram API может привести к временной блокировке аккаунта. Рекомендуется использовать интервал проверки не менее 60 секунд.
